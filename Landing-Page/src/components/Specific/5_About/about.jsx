import React from 'react';
import { Box } from '@mui/material';

const About = () => {
  return (
    <Box
      id="about us"
      sx={{
        height: '100vh',
        paddingTop: '50px',
      }}
    >
      {/* Add content here later */}
    </Box>
  );
};

export default About;
